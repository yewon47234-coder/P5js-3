function setup() {
  createCanvas(600, 400);
  background('#f6f6f8');
  noStroke();
  rectMode(CENTER);
  textAlign(CENTER, CENTER);

  // 색상
  const skin = '#ffe9cf';
  const hair = '#3e2a22';
  const jacket = '#1f3560';
  const shirt = '#d7e6fb';
  const blush = '#ffcdd7';


  
    // -----------------------
  // 5) 뒷머리
  // -----------------------
  
  fill(hair);
  ellipse(300, 180, 220, 240); 
  ellipse(230, 200, 70, 160);
  ellipse(370, 210, 90, 160);


  
 // -----------------------
  // 2) 흰 소매
  // -----------------------
  fill(255);
  ellipse(200, 290, 40, 170);
  ellipse(400, 290, 40, 180);
   rect(300,300, 180, 170, 80);

    
  // -----------------------
  // 1) 자켓
  // -----------------------
  fill(jacket);
  rect(300,300, 180, 170, 70);

  // -----------------------
  // 3) 이너티 (하늘색)
  // -----------------------
  fill(shirt);
  rect(300, 270, 40, 230, 8);

  fill(skin);
  ellipse(200, 210, 24, 36);
  ellipse(400, 210, 24, 36);


  // -----------------------
  // 4) S 로고
  // -----------------------
  fill(255);
  textSize(38);
  textStyle(BOLD);
  text('S', 360, 300);



  // -----------------------
  // 6) 목
  // -----------------------
  fill(skin);
  rect(300, 240, 40, 32, 8);
  
  // -----------------------
  // 7) 얼굴
  // -----------------------
  fill(skin);
  ellipse(300, 160, 120, 140);
  
  // -----------------------
  // 8) 귀 + 귀걸이
  // -----------------------
  fill(skin);
  ellipse(230, 165, 28, 36);
  ellipse(370, 165, 28, 36);

  fill('#ffd700');
  ellipse(230, 185, 8, 8);
  ellipse(370, 185, 8, 8);
  
  // -----------------------
   // -----------------------
// 앞머리 (세 갈래 스타일)
fill(hair);
noStroke();
  
  // 직선 윗부분 (앞머리 라인)
rect(300, 110, 70, 90);

rect(250, 120, 20, 70);

rect(350, 120, 20, 70);






  // -----------------------
  // 10) 눈
  // -----------------------
  fill(255);
  ellipse(265, 170, 28, 18);
  ellipse(335, 170, 28, 18);

  fill(20);
  ellipse(265, 172, 10, 10);
  ellipse(335, 172, 10, 10);

  // -----------------------
  // 11) 쌍꺼풀
  // -----------------------
  noFill();
  stroke(90);
  strokeWeight(1.4);
  arc(265, 162, 30, 10, PI, 0);
  arc(335, 162, 30, 10, PI, 0);
  noStroke();

  // -----------------------
  // 12) 속눈썹
  // -----------------------
  stroke(0);
  strokeWeight(2);
  line(255, 162, 250, 156);
  line(268, 162, 272, 156);
  line(325, 162, 320, 156);
  line(338, 162, 343, 156);
  noStroke();

  // -----------------------
  // 13) 눈썹
  // -----------------------
  fill(45, 35, 30);
  rect(265, 145, 40, 5, 5);
  rect(335, 145, 40, 5, 5);

  // -----------------------
  // 14) 코/입/볼터치
  // -----------------------
  fill('#e6b8a8');
   ellipse(300, 180, 8, 15);

  fill('#e85a7a');
  arc(300, 200, 34, 16, 0, PI);

  fill(blush);
  ellipse(250, 185, 12, 10);
  ellipse(350, 185, 12, 10);
}
